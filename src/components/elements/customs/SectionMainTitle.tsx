import React from 'react';

const SectionMainTitle: React.FC = ({ children }) => (
  <h1 className="mt-6 text-3xl md:text-4xl font-bold text-center md:text-left">
    {children}
  </h1>
);

export default SectionMainTitle;
